# Copy to .env for local runs (never commit .env).
# SKILLS_DIR=./skills
# MCP_TRANSPORT=http        # or "stdio"
# PORT=8000
# HOST=0.0.0.0

# Secrets your skills need are read by `bash` from the env, e.g.:
# LIFX_TOKEN=...
